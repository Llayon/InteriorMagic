import { useEditorStore } from '@/editor/state/store';
export function Toolbar(){const selected=useEditorStore(s=>s.session.selectedId),undo=useEditorStore(s=>s.session.undoStack.length),redo=useEditorStore(s=>s.session.redoStack.length);return <div className="toolbar glass">
  <button aria-label="Undo" disabled={!undo} onClick={()=>useEditorStore.getState().undo()}>↶</button><button aria-label="Redo" disabled={!redo} onClick={()=>useEditorStore.getState().redo()}>↷</button><span className="toolbar-spacer"/>{selected&&<><button aria-label="Rotate left" onClick={()=>useEditorStore.getState().rotate(selected,-1)}>↺</button><button aria-label="Rotate right" onClick={()=>useEditorStore.getState().rotate(selected,1)}>↻</button><button aria-label="Duplicate" onClick={()=>useEditorStore.getState().duplicate(selected)}>⧉</button><button className="danger" aria-label="Delete" onClick={()=>useEditorStore.getState().remove(selected)}>×</button></>}</div>}
